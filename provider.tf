# -----------------------------
# Provider Configuration
# -----------------------------

provider "aws" {
  region = "us-east-1"
  #profile = "default"  # Esto hará que use el perfil por defecto de ~/.aws/credentials
}

